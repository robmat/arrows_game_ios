# Podfile
# Run `pod install` then open arrows.xcworkspace (not .xcodeproj)

platform :ios, '14.0'

target 'arrows' do
  use_frameworks!
  pod 'Google-Mobile-Ads-SDK'
end

target 'arrowsTests' do
  use_frameworks!
end

target 'arrowsUITests' do
  use_frameworks!
end
